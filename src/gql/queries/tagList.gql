#import "../fragments/connection.gql"
#import "../fragments/tag/digest.gql"

query OSSTagList($input: OSSTagsInput!) {
  oss {
    tags(input: $input) {
      ...ConnectionInfo
      edges {
        node {
          ...TagDigest
        }
      }
    }
  }
}
